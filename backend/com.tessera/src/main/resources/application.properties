spring.application.name=com.tessera
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/tessera?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# JWT Configuration
# A chave secreta foi expandida para ser mais segura (>= 256 bits)
app.jwt.secret=teSSer@Ac@d3m1c@S3cr3tK3y2023SuperSecureKey1234567890123456789012345678901234567890
app.jwt.expiration=86400000 

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.tessera=DEBUG 

# File Upload Configuration (Se necessário no futuro)
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB

# Email Configuration
# ATENÇÃO: Substitua 'seu-email@gmail.com' e 'sua-senha' pelas suas credenciais reais do Gmail
# ou pelas credenciais do seu provedor SMTP.
# Para o Gmail, você pode precisar gerar uma "Senha de app".
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=seu-email@gmail.com 
spring.mail.password=sua-senha          
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Para testar a conexão SMTP na inicialização (pode causar falha se as credenciais estiverem incorretas)
spring.mail.test-connection=false 
                                 # Se false e EmailService tiver @Autowired(required=false) no mailSender,
                                 # o app inicia mas o envio de email não funcionará se as configs estiverem erradas.

# WebSocket Configuration (nenhuma propriedade específica aqui, mas relacionado)
# spring.websocket.messagebroker.application-destination-prefix=/app
# spring.websocket.stomp.enabled=true

# Cache (Exemplo - pode ser configurado se necessário)
# spring.cache.type=caffeine